# 🧠 实战经验沉淀库

> 完成任务过程中积累的技巧和认知。目标：遇到新任务时能从这里快速找到解决方案。

---

## 📅 2026-03-23 沉淀

### 🔧 技术技巧

#### 1. API 故障诊断方法论
- **GET 可用 POST 不可用** → 读副本正常，写路径过载（连接池/队列积压）
- **HTTP:000** → TCP 连接建立失败（服务器无响应/连接池耗尽/防火墙）
- **HTTP:502** → 负载均衡 OK，后端应用挂了
- **HTTP:409 duplicate** → 之前超时的请求实际执行成功了（服务器处理了但响应丢失）
- **先测网站 GET → 再测 API GET → 最后测 API POST**（层层递进判断问题层级）

#### 2. A2A 协议发布流程（EvoMap v1.0.0）
```json
{
  "protocol": "gep-a2a",
  "protocol_version": "1.0.0",
  "message_type": "publish",
  "message_id": "msg_<timestamp>_<random>",
  "sender_id": "node_xxx",
  "timestamp": "ISO8601",
  "payload": {
    "assets": [Gene, Capsule, EvolutionEvent],
    "signature": "sha256(sorted_asset_ids_joined_by_pipe)"
  }
}
```
- 心跳是 **REST**（不需要 envelope），POST 端点需要完整 envelope
- asset_id 计算：JSON sort_keys → SHA256（注意 Python 和 JS 的 canonical 差异）
- 签名：HMAC-SHA256 或纯 SHA256 of sorted asset_ids

#### 3. Cloudflared 内网穿透
- Quick tunnel URL 每次重启变，不能绑自定义域名
- Named tunnel 需要 CF API Token + Account.Cloudflare Tunnel:Edit 权限
- 调试：`grep -o 'https://.*trycloudflare.com' /tmp/cloudflared-xxx.log`

#### 4. GitHub CLI 技巧
```bash
gh api repos/OWNER/REPO/compare/BRANCH...BRANCH   # 比较分支
gh api -X POST repos/OWNER/REPO/merge-upstream -f branch=main  # 同步 fork
gh api "search/issues?q=..." --jq '.items[] | ...'  # 搜索 issue
gh api notifications --jq '.[0:5][] | ...'  # 查看通知
```

---

### 🧠 认知升级

#### 5. 降级策略 > 硬等
- **错误做法**：API 超时 → 重试 → 再超时 → 空转
- **正确做法**：超时 → 立即切换到其他可执行任务 → 缓存结果 → 恢复后重试
- **原理**：外部服务的恢复时间不可控，但自己的时间可控

#### 6. 请求可能成功但响应丢失
- HTTP ReadTimeout ≠ 请求失败
- 409 duplicate 证明之前的超时请求实际到达并执行了
- **经验**：发布类操作应该先检查是否已存在（通过 asset_id 或 duplicate 检测）

#### 7. 频率自适应
- 外部服务宕机时，轮询频率应该降低而不是保持不变
- 反思任务连续相同结论时应该跳过（防空转）
- **公式**：异常持续时间 × 轮询频率 = 浪费的 token

#### 8. 任务完成 ≠ 经验沉淀
- 完成 19 个任务但如果没记录学到什么 = 从零开始下一个类似任务
- **必须**：每个任务完成后追加 "学到了什么" 到 .learnings/
- **格式**：场景 → 做了什么 → 结果 → 学到了什么 → 下次怎么用

#### 9. 归档循环（复利模式）
- 每完成 12 个短期任务 → 归档 → 基于新经验生成下一轮 12 个
- 新任务质量应该比上一轮更高（因为有了经验加持）
- **不归档 = 经验归零**

---

### 📐 工程模式

#### 10. sync_completed_tasks.py v2 修复
- **Bug**：跨日期同 ID 任务被虚假标记（S-01 今天 ≠ S-01 昨天）
- **Fix**：只扫描当日 memory + 只匹配当日 plan 中有效 ID
- **通用模式**：任何"跨实体同名"的匹配都需要限定上下文域

#### 11. 心跳 + 降级 + 缓存 三位一体
```
heartbeat():
    if api_timeout:
        try_read_only_api()  # 确认是写问题还是全面问题
        if read_ok:
            return cached_data  # 用上次成功的数据
        else:
            return fallback_status
    else:
        update_cache()
        return fresh_data
```

#### 12. 协议变更检测
- 从 API 错误响应中提取协议文档 URL（如 400 response 中的 `/a2a/skill?topic=envelope`）
- 对比自己使用的格式和官方文档的差异
- **经验**：400 比 502 有用 — 400 说明服务器在工作，只是格式不对

---

## 📅 2026-03-24 沉淀

### 🔧 技术技巧

#### 13. Task-Executor Cron 设计模式
- **问题**: cron 体系全是观察者，没有行动者执行 project-plans 中的任务
- **方案**: 创建 isolated session cron，每小时执行一个原子任务
- **流程**: 读 project-plans → 取第一个 `- [ ] S-` → 执行 → 标记 `[x]` → 写 memory log
- **关键参数**: sessionTarget=isolated, timeout ≥ 300s（前两次 180s/300s 超时，第三次 94s 成功）
- **教训**: isolated session 首次运行有冷启动开销，超时要给够

#### 14. EvoMap 任务自动跳过策略
- **规则**: 如果 evomap.ai 超时或返回错误，自动跳过 EvoMap 相关任务
- **识别**: 任务描述含 "EvoMap"/"capsule"/"A2A" → 先测试 Hub 连通性 → 不通则跳
- **降级**: 跳过后取下一个非 EvoMap 任务执行，不空转

#### 15. Dashboard 数据源确认
- Dashboard 解析的是 `docs/project-plans-YYYY-MM-DD.md`，不是 `active.md`
- 完成任务后需同步标记 `[x]`，否则 Dashboard 显示未完成
- `sync_completed_tasks.py` v2 已修复跨日期虚假标记问题

---

## 🔑 核心原则（从失败中提炼）

1. **外部依赖故障时，优先做自己的事** — 不要等别人恢复才开始工作
2. **每个请求都可能成功（即使超时）** — 发布前先检查重复
3. **频率要自适应** — 宕机时降低频率，恢复后提高
4. **经验必须显式记录** — 不记录 = 没学到
5. **复利循环不能断** — 完成→归档→学习→生成新任务→完成
6. **Cron isolated session 有冷启动** — 首次运行比预期慢 2-3x，超时给够
7. **降级不空转** — 跳过不可执行的任务，立即换下一个，不浪费执行窗口

## 📅 2026-03-28 沉淀（第2轮）

### 🔧 第2轮 — 技术类任务经验
**日期**: 2026-03-28 | **涉及任务**: 1个

**任务清单**:
  - S-02: 搜索 arXiv 最新论文准备 capsule 素材（关联 M-01-4）

**洞察**: 技术类任务共 1 个，涉及代码/工具/研究。常见模式：先搜索现有方案 → 评估可行性 → 实现最小可用版本 → 验证效果。

**行动项**: 下次技术任务：优先搜索 `.learnings/EXPERIENCE.md` 有无同类经验，复用已验证方案。


### 🔧 第2轮 — 其他类任务经验
**日期**: 2026-03-28 | **涉及任务**: 1个

**任务清单**:
  - S-03: 维持心跳间隔 ≤15 分钟，验证节点在线（关联 M-01-1）

**洞察**: 其他类任务共 1 个。

**行动项**: 考虑为这类任务建立标准化流程。


## 📅 2026-03-29 沉淀（第2轮）

### 🔧 第2轮 — 策略类任务经验
**日期**: 2026-03-29 | **涉及任务**: 1个

**任务清单**:
  - S-09: 研究 1 个可产品化的技能方向（关联 M-02-4）

**洞察**: 策略类任务共 1 个，涉及规划/架构/方向。常见模式：调研现状 → 分析差距 → 制定方案 → 执行验证。

**行动项**: 下次策略任务：先回顾已有经验库，避免重复探索。


### 🔧 高通量执行经验 (2026-03-29)
**最有效工具组合**:
1. exec + python3 (直接执行，最快)
2. 文件 write/edit (无依赖，即时生效)
3. gh api (GitHub 操作)
4. curl -sL (API 调用)

**最有效工作方式**:
1. 批量执行: 同类任务合并到一个 exec
2. 直接文件操作: 比通过脚本快 10x
3. 双验证: 内容+格式
4. 不阻塞: 限流时立即切换

**经验教训**:
1. 不要等外部 API - 立即切换任务
2. 验证比执行更重要 - 假完成是最大的浪费
3. 任务粒度要小 - ≤5min 轻量，≤15min 中等
4. 心跳是后台任务 - 不应打断主执行流

---

## 📅 2026-03-31 沉淀

### 🎯 Skill 固化 SOP 经验

#### 1. 创建 understand-project 技能的过程

**背景**：用户执行 `/understand` 分析代码项目时，我没有调用原版 Understand-Anything 工具，而是自己用 `find/cat/ls` 来"分析"，效果差了 30 倍。

**过程**：
1. 发现问题：用户指出效果差，我才意识到有专门的分析工具
2. 研究原版：阅读 Understand-Anything 的 SKILL.md（28KB，7 阶段流水线）
3. 创建技能：写 SKILL.md + run.py
4. 测试验证：多次运行确保结果一致
5. 发现问题：示例数据不一致、语言检测不完整
6. 迭代改进：扩展语言检测、改进函数提取

#### 2. 关键教训

**❌ 错误做法**：
- 没有检查可用的技能就自己造轮子
- 示例数据随手写，没有和实际代码同步
- 只测试一次就报告完成
- 数字不严谨（1,231 vs 1,207）

**✅ 正确做法**：
- 收到任务先检查有没有现成工具（`~/.openclaw/skills/`）
- 示例数据必须从实际运行结果中提取
- 多次测试确保结果稳定一致
- 数字必须精确，不能估算

#### 3. Skill 固化检查清单

创建新 Skill 时必须检查：

```markdown
□ 1. 是否有现成的工具/技能可以调用？
□ 2. SKILL.md 中的示例是否来自实际运行？
□ 3. 是否多次测试确保结果一致？
□ 4. 触发词是否足够多，提高调用概率？
□ 5. 是否有错误处理和降级逻辑？
□ 6. 是否记录了版本号和更新时间？
```

#### 4. 技能对比：原版 vs 我的

| 维度 | 原版 Understand-Anything | 我的 understand-project |
|------|------------------------|----------------------|
| 节点数 | 7,967 | 5,920 |
| 边数 | 7,942 | 7,938 |
| 语言检测 | 13 种 | 5 种 |
| 架构层 | 8 层 | 23 层 |
| 学习路线 | 12 步 | 无 |
| 执行方式 | 子代理并行 | 单进程 |

**结论**：我的技能是简化版，够用但不完整。原版更准确是因为使用子代理并行分析。

#### 5. 代码分析类 Skill 的最佳实践

1. **语言检测**：必须覆盖项目的主要语言（Python, TypeScript, JavaScript, Go, Rust, Java...）
2. **函数提取**：不同语言的函数定义模式不同，需要分别处理
3. **导入分析**：Python 用 `from/import`，JS/TS 用 `import/from`，Go 用 `import`
4. **架构层识别**：按目录分组是最简单有效的方法
5. **学习路线**：从入口文件开始，按依赖关系组织

#### 6. 下次改进方向

- [ ] 添加 TypeScript/JavaScript 深度分析
- [ ] 添加学习路线生成
- [ ] 添加框架检测（FastAPI, Django, Express...）
- [ ] 支持增量分析（只分析变更文件）
- [ ] 添加可视化仪表盘

---

### 📝 规则沉淀

#### 规则 1：先检查再动手
```
收到任务 → 检查 ~/.openclaw/skills/ → 有就调用 → 没有再自己做
```

#### 规则 2：示例必须真实
```
写 SKILL.md → 运行代码 → 把输出复制到文档 → 不要手写示例
```

#### 规则 3：多次测试
```
第 1 次运行 → 记录结果
第 2 次运行 → 对比结果
结果一致 → 报告完成
结果不一致 → 找原因修复
```

#### 规则 4：数字要精确
```
1,207 个文件 ✅
约 1,200 个文件 ❌
1,231 个文件 ❌（写错了）
```
